/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['zqkydokgcil2ctgj.public.blob.vercel-storage.com'],
  },
}

module.exports = nextConfig
